const initialState = [
    {
        id: "1", //stays a string until we get server-assigned IDs
        title:"Sample title",
        description: "Sample description",
        content:"Hello world!"
    }
];

export default initialState;